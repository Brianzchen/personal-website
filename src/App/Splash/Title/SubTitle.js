import React from 'react';

import colors from 'lib/colors';

const style = {
  color: colors.white,
};

const SubTitle = () => (
  <i style={style}>
    Front-end web developer
  </i>
);

export default SubTitle;
