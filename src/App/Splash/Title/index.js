import React from 'react';

const style = {
  flex: 1,
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
};

const Title = () => (
  <div style={style}>
    Brian Chen
  </div>
);

export default Title;
