import React from 'react';

const style = {
  flex: 1,
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
};

const Title = () => (
  <div style={style}>
    <h1>
      Brian Chen
    </h1>
  </div>
);

export default Title;
