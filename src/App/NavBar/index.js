import React from 'react';

const style = {
  position: 'fixed',
  top: 0,
  left: 0,
  right: 0,
  height: '32px',
  background: 'blue', // TODO replace
};

const NavBar = () => (
  <div style={style}>
    NavBar
  </div>
);

export default NavBar;
